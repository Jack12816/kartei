# chat-unread

A fictional chat server module tracking unread message counters. Its
`tmp/` directory holds throwaway dependency checkouts — nested git
repositories the indexer must never descend into.
